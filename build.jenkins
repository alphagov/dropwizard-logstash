pipeline {
    agent {
        docker {
            image 'govukverify/java8:latest'
            additionalBuildArgs "--pull"
        }
    }

   stages {
       stage('Build') {
           steps {
               sh './gradlew build'
           }
       }
   }
}
