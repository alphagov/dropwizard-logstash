server:
  applicationConnectors:
    - type: http
      port: 0
  requestLog:
    type: classic
    appenders:
      - type: logstash-console

logging:
    appenders:
        - type: logstash-console
